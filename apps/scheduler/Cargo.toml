[package]
name = "scheduler"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
chrono = "0.4"
shared-binance = { path = "../../crates/shared-binance" }
shared-domain = { path = "../../crates/shared-domain" }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net"] }
