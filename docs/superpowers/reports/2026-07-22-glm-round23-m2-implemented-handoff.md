# GLM Martingale Core Round 23 — M2 Implemented + M1+M2 Combination Framework

**Date:** 2026-07-22
**Branch:** `glm-martingale-core-round23`
**Machine state:** `BLOCKED_ENGINE_DATA_OR_EXECUTION`

## 0. Headline

Implemented the M2 (Depth Replenishment + Aggressor Flow) signal layer per plan §8 M2, downloaded bookDepth data (80 days available, full-year download running), and built the M1+M2 combination framework with AND/OR gate modes. The M1+M2 OR-gate produces trades but the 80-day bookDepth window is too short for reliable evaluation. **No target claimed** — the combination needs the full-year bookDepth download to complete.

## 1. M2 signal layer — `crates/r23-replay/src/m2_signal.rs`

Implements plan §8 M2:
- **Depth imbalance**: `I(level) = (bid_notional - ask_notional) / (bid_notional + ask_notional)` at 1% and 2% levels (Binance provides integer 1-5% levels; plan's 0.2%/1.0% mapped to available 1%/2%).
- **Replenishment**: bid depth rose in the last 3 snapshots.
- **Aggressor flow decay**: signed volume deceleration (stubbed when aggTrades unavailable; full implementation needs aggTrades download).
- `compute_m2_states(depth, trades, window)` → `Vec<M2State>`.
- Unit tests pass (imbalance symmetry, empty handling).

## 2. bookDepth data download

- `scripts/r23_download_binance_vision.py` extended to download bookDepth (same checksum+manifest pipeline as metrics).
- Downloaded 80 days for 5 symbols (BTC/ETH/LINK/BNB/SOL); extended download through 2024-06 launched in background (~335 days target).
- bookDepth schema verified: `timestamp,percentage,depth,notional` with negative=bid, positive=ask.

## 3. M1+M2 combination — `bin/r23_m1m2_combo.rs`

- AND-gate: FO fires only when M1 (price_ext + OI) AND M2 (depth replenishment) both confirm.
- OR-gate: FO fires when either M1 or M2 confirms (more trades).
- 5-symbol combination, 5 cold starts, DSR(n=1)/PBO, 3-tier judgment.

### Results (OR-gate, fo=25%, 80-day bookDepth window)
| Cold start | Comp % | Ann % | DD % |
|---:|---:|---:|---:|
| cs0 | -5.05 | -21.05 | 17.83 |
| cs30 | -9.73 | -52.63 | 18.65 |
| cs60 | +2.80 | +65.48 | 3.62 |

- 1/3 positive, DSR -6.17, PBO 0.81. **Window too short (80d) for reliable evaluation.**
- AND-gate (earlier): 0 trades (too strict on the short window).

## 4. Honest status

**M2 is implemented and integrated.** The bookDepth data covers only 80 days so far — the combination cannot be reliably evaluated until the full-year download completes. The early results are noisy (80-day windows are too short for stable statistics). The framework is ready; the data is the bottleneck.

## 5. Three-tier judgment

All tiers `false`. The M1+M2 combination has not been validated on a ≥365-day window yet (bookDepth data insufficient). No target claimed.

## 6. What remains (concrete)

1. **Wait for bookDepth full-year download** (running; ETA ~1-2h for 335 days × 5 symbols).
2. **Re-run M1+M2 combination** on the ≥365-day window with both AND and OR gates.
3. **Download aggTrades** (6 symbols) for the full aggressor-flow signal (currently stubbed).
4. If M1+M2 on full window achieves PBO<0.5 + DSR>0 + tier ann/DD/cold-start → claim.

## 7. Registry + state

- `phases_completed`: [R0, R1, R4, G1, G2, R8].
- M2 module + m1m2_combo binary + bookDepth downloader all committed.
- 18 unit + 18 canary tests pass.

---

*Generated by GLM on `glm-martingale-core-round23`. State: `BLOCKED_ENGINE_DATA_OR_EXECUTION`. M2 implemented; bookDepth download in progress; M1+M2 combination framework ready. No target claimed (bookDepth window 80d < 365d required).*
