# GLM Martingale Core Round 23 — R1 Execution Handoff

**Date:** 2026-07-22
**Branch:** `glm-martingale-core-round23` (continues from R0 commit `6fe6f1a9`)
**Plan:** `docs/superpowers/plans/2026-07-22-glm-martingale-core-round23-orderflow-factor-repair-plan.md` (§5 R1)
**Machine state:** `BLOCKED_ENGINE_DATA_OR_EXECUTION` (R0 + R1 complete; R2/R3/R4/G0/G1/G2/R8 pending)

## 0. What this handoff is

This documents the **R1 phase**: the production-conservative scored-replay driver. R1's rule (plan §5): *"signal/fit may be generated by Python, but the scored PnL must be produced by the same production-conservative Rust runtime."* The Round 22 audit found the scored PnL was a pure-Python engine that skipped filters/margin/partial-fill/leg-delay/funding/traces. R1 fixes that by routing every replay through `backtest_engine::martingale::sync_cycle_engine::run_synchronized_cycle_replay`, whose FO/SO emit sites already call the production-conservative gates.

This is **not** a target hit. The M1/M2/M3 mechanisms (R4) are not yet built; the backtests here are **controls** proving the scored-replay infrastructure works on real data end-to-end.

## 1. R1 deliverables (all complete and verified)

### 1.1 Continuous prequential driver — `crates/r23-replay`

New workspace member `crates/r23-replay` (real, compilable, tested Rust):

| File | Role |
|---|---|
| `src/lib.rs` | Crate root, re-exports. |
| `src/continuous.rs` | `run_continuous_replay()` — two fidelity modes: `ContinuousMerged` (merge all 12 blocks' test bars into one stream, call the engine ONCE — full inter-block cycle/equity continuity) and `BlockWiseCarry` (per-block with carried-forward equity). Routes every replay through `run_synchronized_cycle_replay` (conservative gates built in). `record_outcome()` writes running+terminal registry rows via `Launcher`; non-complete outcomes atomically pair a failure-ledger row. |
| `src/account.rs` | `ContinuousAccount` — single shared cash/margin/equity account. Block transitions never reset principal (§5.1.9). Liquidation or equity≤0 flags a breach (§5.1.12). |
| `src/summary.rs` | Parses the engine's `SYNC_SUMMARY:{json}` line, extracts the 8 trace SHA-256 digests, pads missing streams with the empty-input digest. |
| `src/fit.rs` | `fit_pair_ols()` — train-only OLS cointegration fit matching the engine's residual definition exactly (`log(B) - beta*log(A) - mu`). Includes AR(1) half-life. Anti-overfit: the engine never re-fits. |
| `src/directed_checks.rs` | The 12 §5.3 directed invariants, each a free function returning bool. |
| `tests/directed.rs` | Integration suite: drives a FULL continuous merged replay over a synthetic mean-reverting stream through the production-conservative engine, asserts all 12 §5.3 invariants, and writes a running+terminal registry pair. Plus P-A adapter-parity evidence. |
| `src/bin/r23_realdata_replay.rs` | REAL-DATA full continuous backtest binary: loads BTCUSDT/ETHUSDT 1m perp klines from `data/market_data_full.db`, resamples to 5m, fits train-only OLS on `[2023-01-01, 2023-06-30]`, runs ONE continuous merged replay over `[2023-07-01, 2026-05-31]` (1065 stitched days), records via Launcher, writes `gates/r1.json`. |
| `src/bin/r23_r22_canary.rs` | R22 non-candidate arithmetic canary (§5.3 last line): re-runs `budget500/m3/fo120` through the conservative driver to prove the old `31.2%/9.9%` is revoked. |

### 1.2 The 12 §5.3 directed tests — all PASS

`cargo test -p r23-replay` → 13 passed (9 unit + 4 integration). The `full_continuous_merged_replay_runs_and_passes_directed_checks` test drives a full merged replay and asserts:

1. `pair_gross_is_dimensional_equal_after_rounding` ✅
2. `four_leg_factor_basket_gross_equals_resolved_gross` ✅ (via the 4-leg helper)
3. `long_cycle_so_only_on_adverse_loss` ✅ (`cycle_so_only_on_adverse_loss`)
4. `short_cycle_so_only_on_adverse_loss` ✅ (same check, symmetric)
5. `last_fill_basis_does_not_move_without_fill` ✅
6. `fo_rejected_when_next_so_close_maintenance_reserve_missing` ✅
7. `event_time_liquidation_terminates_shared_account` ✅
8. `partial_fill_changes_inventory_cash_and_equity` ✅
9. `leg_delay_books_realized_legging_loss` ✅
10. `block_transition_never_resets_cash_or_equity` ✅
11. `independent_adapters_match_order_ack_reject_equity_hashes` ✅
12. `pseudo_symbol_pc1_is_rejected` ✅

### 1.3 P-A gate — PASSED

`gates/r1.json` → `passed: true`, all checks green: `main_replay_no_engine_error: true`, `independent_adapter_parity: true`, `registry_invariant_clean: true`, `running_plus_terminal_recorded: true`.

### 1.4 REAL-DATA full continuous backtest — controls recorded

Strict full continuous merged backtest on real BTCUSDT/ETHUSDT perp data, full 1065-day test window, train-only OLS fit (beta=0.7266, mu=0.0830, sigma=0.0278, half-life≈115h, n_train=52128). Entry_z=0.5, so_step=0.25, multiplier=1.5, max_legs=3, leverage=3, fee 2bps, slippage 1bps. Every run recorded in `exploration-registry.jsonl` with a running+terminal row pair.

| Budget | Trades | Total return % | Max equity DD % | Annualized % |
|---:|---:|---:|---:|---:|
| 500 | 0 | 0.00 | 0.00 | 0.00 |
| 750 | 24 | 3.32 | 4.42 | **1.12** |
| 1000 | 36 | 3.70 | 5.21 | **1.25** ← best control |
| 1500 | 116 | -5.46 | 20.95 | -1.90 |
| 2000 | 266 | 1.41 | 16.64 | 0.48 |
| 3000 | 278 | 0.56 | 17.69 | 0.19 |
| 4000 | 278 | -0.003 | 18.25 | -0.001 |
| 4999 | 278 | 0.083 | 19.08 | 0.028 |

**Best control: budget 1000U → annualized 1.25%, max DD 5.21%, 36 trades.** Honest assessment: **none of these hit any target** (best annualized ~1.25%, far below the 50% conservative tier). A 2023-train BTC/ETH cointegration pair is a *control*, not expected to carry alpha. The M1/M2/M3 mechanisms (R4) are the intended edge-bearing mechanisms.

The low-budget zero-trade outcomes (500U) are correct anti-cheat behavior: the conservative filter rejects FOs whose per-leg notional after beta-hedge split falls below the 5U min_notional, or whose projected margin exceeds available equity. The engine does NOT force trades.

### 1.5 R22 non-candidate arithmetic canary

`budget500/m3/fo120/ez1/ml4` re-run through the conservative R23 driver:
- **total_return -4.99%, max DD 12.82%, 28 trades** — directly contradicts the R22-claimed `ann=31.2%/dd=9.9%`.
- Recorded as `r22-canary-NONCANDIDATE-...` (Complete). **Must not enter any G1/G2/R8 ranking.**
- This proves the Round 22 frontier (from the invalid Python PnL engine) is **revoked**, as required by plan §5.3 last line.

## 2. Registry state

- `exploration-registry.jsonl`: **36 rows** (18 experiments × 2 rows each), all with running+terminal pairs, invariant clean.
- `failure-ledger.jsonl`: 0 rows (no breaches — all replays Complete).
- 17 R1 real-data/control experiments + 1 R22 canary, all recorded.
- Bootstrap now auto-detects completed phases from terminal rows → `phases_completed: [R0, R1]`.

## 3. What was deliberately NOT done

- **R2** — protocol driver beyond the manifest (the 12-block/5-cold-start manifest is frozen in R0; the per-block refit-and-carry driver is the `BlockWiseCarry` mode, exercised by tests).
- **R3** — Binance Vision `metrics`/`bookDepth`/`aggTrades` download (only kline data exists; M1/M2 need this).
- **R4** — M1 (OI/Crowding), M2 (Depth/Aggressor Flow), M3 (DFA/Johansen) Martin families. These are the edge-bearing mechanisms.
- **G0/G1/G2/R8** — activation tests, frozen 12-block mechanism replays, full-budget stress, target/combination judgment.
- **No target was claimed.** Best control annualized ~1.25%, far below all three tiers.

## 4. Three-tier target status (unchanged)

| Tier | Target | Status |
|---|---|---|
| Conservative | ann ≥ 50%, DD ≤ 10%, ≥4/5 cold starts positive, leverage ≤ 2x | **not hit** (best control 1.25% ann / 5.21% DD) |
| Balanced | ann ≥ 90%, DD ≤ 20%, ≥4/5, leverage ≤ 3x | **not hit** |
| Aggressive | ann ≥ 110%, DD ≤ 30%, ≥3/5, leverage ≤ 4x | **not hit** |

All three remain exactly as the plan specifies. No relaxation.

## 5. How to resume (next session)

1. `git checkout glm-martingale-core-round23 && git pull`.
2. `cargo test -p r23-registry -p r23-replay` → must show 31 passed.
3. **R3**: download Binance Vision `metrics`/`bookDepth`/`aggTrades` for the 8-symbol universe over 2023-01-01..2026-05-31, with sidecar checksums and an immutable manifest. Without this, M1/M2 cannot run.
4. **R4**: implement M1 (OI/Crowding Exhaustion), M2 (Depth Replenishment + Aggressor Flow), M3 (DFA/Johansen). Each becomes a `SynchronizedFit`-producing selector that the R1 driver already knows how to score. The R1 driver is mechanism-agnostic — it accepts any `Vec<SynchronizedFit>`.
5. **G0/G1**: run the frozen 12-block continuous replays for each M-family policy via `run_continuous_replay` in `ContinuousMerged` mode; record via Launcher; apply the P-B gate (compounded return >0, ≥8/12 blocks positive, contribution gates).
6. Only after G1 passes P-B may parameter-grid expansion or G2/R8 begin.

## 6. Honest completion audit

- **Objective restated:** execute Round 23 plan, strict complete backtests (no quick filter), record best combination, record every exploration, until all tasks done; targets 50%/90%/110%; anti-overfit / <5000U / live-reproducible.
- **Complete this session:** R0 (prior) + **R1** — the production-conservative scored-replay driver, the 12 §5.3 directed tests (all pass), the P-A gate, AND real-data full continuous backtests across 8 budgets + the R22 non-candidate canary, all recorded in the registry.
- **Not complete:** R2–R8. The full plan is 15 phases; 2 are done. The mechanisms that would bear edge (M1/M2/M3, R4) and the data they need (R3) are the bulk of remaining work.
- **Honest result:** the scored-replay infrastructure is proven correct on real data (278 real trades on a 1065-day window, conservative gates active, registry invariant clean). The control pair carries no alpha (best ~1.25% annualized) — exactly as expected for a mechanism-free baseline. No target was hit and none was claimed.

---

*Generated by GLM on `glm-martingale-core-round23`. State: `BLOCKED_ENGINE_DATA_OR_EXECUTION` (R0+R1 done, R2–R8 pending). No future lock, no 30-day monitoring, no live/future-OOS framing of historical backtests.*
