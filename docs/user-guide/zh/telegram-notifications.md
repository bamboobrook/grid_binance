# Telegram 通知

把 Telegram 绑定完成后，关键运行提醒会同时出现在网页和聊天里。

登录后打开 `/app/telegram`。这里是 Telegram 绑定和送达状态的唯一入口。

先在网页生成一串短时效绑定码，再把它发送给 Telegram 机器人完成绑定。

一个用户只能绑定一个 Telegram 身份，绑定码会在短时间后自动失效。

Telegram 会同步这些通知：

- 策略启动、暂停、异常
- API 凭证失效或权限问题
- 会员到期提醒
- 充值确认
- 逐笔成交盈亏摘要
