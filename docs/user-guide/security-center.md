# Security Center

Manage passwords, TOTP, and session review without losing visibility into account posture.

Use a unique password, enable TOTP, disable TOTP when required, and revoke stale sessions whenever device trust changes.

Password changes and TOTP lifecycle actions should be submitted over protected POST flows rather than URL query toggles.

Binance API secrets stay encrypted and masked after save. Withdrawal permission must remain disabled.
