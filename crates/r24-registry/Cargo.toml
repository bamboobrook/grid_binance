[package]
name = "r24-registry"
version = "0.1.0"
edition.workspace = true
description = "Round 24 fail-closed experiment registry, launcher, and evidence validator"

[lib]
name = "r24_registry"
path = "src/lib.rs"

[dependencies]
anyhow.workspace = true
chrono.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
tempfile = "3"

